<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 
"-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member">

	<select id="getMemberList" resultType="com.pro3.dto.Member">
		select * from member order by resdate desc
	</select>
	
	<select id="getMember" parameterType="String" resultType="com.pro3.dto.Member">
		select * from member where id=#{id}
	</select>
	
	<select id="memberCount" resultType="int">
		select count(*) from member
	</select>
	
	<insert id="insMember">
		insert into member values(#{id}, #{pw}, #{name}, #{email}, #{tel}, #{addr1}, #{addr2}, #{postcode}, #{birth}, deafult})
	</insert>
	
	<update id="upMember">
		update member set pw=#{pw}, name=#{name}, email.#{email}, tel=#{tel}, addr1=#{addr1}, addr2=#{addr2}, postcode=#{postcode}, birth=#{birth} where id=#{id}
	</update>
	
	<delete id="delMember">
		delete from member where id=#{id}
	</delete>
</mapper>